# v2.5 阶段 6A 实施记录

> 状态：已关闭；6A-1～6A-3 与真实 Compose/重启/离线恢复门禁均通过完整 CI
> 更新日期：2026-08-10
> 前置：阶段 5 工程门禁已由 GitHub Actions run `31063896157` 关闭；CPU/GPU runtime
> semantic 等价与领域代表性签字继续作为发布债务

## 1. 阶段目标

阶段 6 会持续增加数据角色、质量、统计、预测、多数据集与受控并行能力。扩展前必须先建立
运行边界：一个 TaskRun 从创建到终态只能使用创建时经过 Host 准入的 capability/tool 目录，
不能因为注册表、MCP `tools/list_changed` 或部署 profile 在运行中变化而让计划无审计漂移。

6A-1 建立不可变目录和消费边界；6A-2 在该边界上接入受治理目录换代和部署 profile
可用性；6A-3 建立共享预算、固定数据版本、取消树和统一 Evidence Ledger，并仅对同一
ready frontier 内互不依赖的受治理只读工具开启有界并行。

## 2. 6A-1 实际交付

- SQLite schema v9 新增 `capability_catalog_snapshots`。TaskRun 创建时在同一事务内保存唯一、
  内容 hash 标识、应用与数据库触发器双重保护的不可变目录；readiness、迁移校验、受保护回滚
  和工作区备份/恢复行数清单同步升级；
- `AgentToolRegistry` 从单源 MCP Tool descriptor 生成 `chatbi-capability-catalog-v1`：固定
  capability、tool name、service route、tool version、完整 contract hash、风险、权限、Artifact
  后置条件和只读/幂等属性；目录顺序确定，可稳定计算内容 hash；
- 新 TaskRun 原子绑定当前目录。v9 前创建且尚未结束的兼容任务在首次恢复时只允许补写一次，
  后续禁止替换；SSE `meta` 暴露 snapshot ID 与 content hash，便于审计和恢复核对；
- Planner、澄清后的重新规划和 Observation Replanner 都读取 TaskRun 固定的 capability 列表；
  模型可见工具同时按固定 tool name 集合过滤。注册表新增工具不会进入运行中的任务，只对新任务
  生效；
- 恢复时逐项核对已冻结工具是否仍存在，以及 service route、tool version、contract hash 和
  capabilities 是否一致。缺失或漂移会以稳定 `capability_catalog_drift` 失败关闭，不允许模型
  心算、静默换工具或用新目录覆盖旧目录。

## 3. 回归覆盖

- migration/readiness 覆盖 schema v9 表与 checksum 漂移；
- TaskStore 覆盖原子保存、canonical hash、调用方对象修改不反写、幂等读取、替换冲突和数据库
  UPDATE 触发器拒绝；
- Registry 覆盖新增工具对旧 TaskRun 隐藏、工具缺失失败关闭以及 Tool contract hash 漂移；
- Planner/Replanner 保留无显式快照调用的兼容入口，生产 Agent 循环显式传入固定目录；
- 定向 migration、TaskStore、备份、Registry、Planner/Replanner、v3–v8 升级及恢复漂移
  失败关闭回归当前为 **107 条通过**。
- 全仓 `ruff check`、变更源文件 strict `mypy`（仅屏蔽既有第三方 stub/冗余 cast 噪声）与
  `git diff --check` 通过；当前沙箱仍复现项目已记录的 AnyIO/asyncio 第二次线程池任务不调度，
  真实线程池全量 Agent/协议回归交由 CI 验证。

## 4. 6A-2 实际交付

- 官方 SDK Session 的消息缓冲同时接收标准 `notifications/tools/list_changed`；项目内 MCP
  Server 宣告 `tools.listChanged=true`。Gateway 收到通知后重新执行完整 `tools/list`，逐项
  比对 Host 静态 allowlist、输入/输出 schema、capability、风险、权限、Artifact 后置条件与
  版本 hash；缺失、未知、重复或漂移目录以 `mcp_catalog_drift` 失败关闭，不能动态接纳第三方
  或未审查工具；
- 每个新 TaskRun 在写入不可变 snapshot 前必须完成全部远端 Server 发现。快照新增实际验证过的
  分服务目录 hash；目录不可连接时返回可重试 `capability_catalog_unavailable`，契约漂移返回
  不可重试 `capability_catalog_drift`。运行宿主持续监听目录通知，但已创建 TaskRun 仍只消费
  自己冻结的 capability/tool 集合；
- 新增 `AGENT_CAPABILITY_PROFILES`，只接受 `stats`、`forecast`、`browser`、`gpu` 的精确、
  无重复清单，默认 `stats,browser`；GPU RAG 部署会自动投影 `gpu` 已启用。统计能力和浏览器
  截图能力在对应 profile 未启用时携带 `allowed=false`、`required_profile` 与稳定
  `profile_not_enabled` 原因；
- 预测 profile 目前尚无受治理 Forecast Tool，因此无论是否启用都不伪装可执行：禁用时标记
  `profile_not_enabled`，启用后标记 `tool_unavailable`。GPU 当前是知识检索运行加速 profile，
  没有虚构独立 Planner capability；其启用状态与关联能力列表仍进入快照供运维审计；
- Planner 与 Replanner 保留完整目录作为解释上下文，但确定性校验和工具暴露只接受
  `allowed=true` 的 capability/tool。能力缺失会产生明确失败或受约束重规划，不允许退回模型
  心算。

## 5. 6A-2 回归覆盖

- Tool 通知缓冲、SDK capability 宣告、同契约换代 hash 稳定以及变更后契约漂移失败关闭；
- profile 禁用、预测工具缺失、冻结工具过滤、远端分服务 hash 写入快照；
- Planner/Replanner 对“目录中存在但 unavailable”的能力失败关闭；Agent 集成覆盖远端发现先于
  TaskRun 创建，以及恢复时继续核对冻结目录；
- 当前可独立执行的定向/分文件回归 **48 条通过**，变更 Python 文件 `ruff check`、strict
  `mypy` 与 `git diff --check` 通过。本地全协议/Agent 文件仍触发阶段 5 已记录的 AnyIO/asyncio
  第二次线程池任务不调度，完整真实异步链交由 CI 验证。

## 6. 6A-3 实际交付

- SQLite schema v10 新增 `task_execution_scopes`、`task_dataset_bindings`、
  `task_cancellation_nodes` 和 `evidence_ledger_entries`。TaskRun 创建时原子固定调用/并行预算、
  根取消节点和当前项目数据集版本；不可变触发器、readiness、备份/恢复和受保护回滚同步升级；
- `reserve_parallel_invocations` 在一个 SQLite 事务中对整个批次做 ready-step 绑定、固定
  data version 校验、共享预算预留、Invocation/取消分支创建和步骤启动；任一分支不合法时
  整批不落库，并发调用不能各自拥有预算；
- 调度器只并行执行不同 ready step 的 `low/medium` 风险、只读、幂等、非 destructive/
  open-world MCP Tool；写操作、需审批工具、异常条件分支、图表与报告交付保留原串行语义；
- 同批分支携带同一起始 `evidence_ledger_version` 和数据版本 hash，且各自携带 Host 创建的
  `cancellation_node_id`。分支可并发完成，但结果仍由 Host 按模型调用顺序原子提交，形成唯一、
  连续、追加式 Evidence Ledger，不允许 Server 直接影响最终答复；
- TaskRun 取消会传播到根节点和所有活动分支，未确认结果转为 `unknown` 并禁止自动恢复。
  `transform_dataset` 产生的同项目衍生版本可以追加绑定，任务开始后登记的无关数据集和其他
  分支占用的版本引用会失败关闭；
- 新增 `AGENT_MAX_PARALLEL_TOOLS`，默认为 4、边界为 1–16；顺序调用与并行批次共享同一
  持久化 `max_tool_calls`，根 Compose 将该上限显式投影到 API 容器。
- TaskRun 详情将共享预算、并行上限、数据版本数、当前数据 hash、根取消状态和
  Evidence Ledger 版本投影为 `execution_control`；每条工具审计增加并行标记、分支节点、
  取消状态、数据版本 hash 和 Ledger 序号。React 协作面板只展示这些持久化事实，不推测并行状态。

## 7. 6A-3 回归覆盖

- 存储层覆盖并行批次的全有或全无预留、超预算拒绝、运行后新数据集拒绝、衍生版本追加、
  乱序完成/统一 Ledger 总序、重复提交拒绝和取消传播；
- Agent 集成替身会在首个分支等待第二个分支启动，确认调用真正重叠，同时验证两个
  Invocation、两条 Evidence、同一数据版本、独立取消节点和连续 Ledger 序号；
- 当前 migration/TaskStore/协议/定义与服务/血缘与记忆升级/并行 Agent 定向回归共 **94 条通过**；
  独立 v1→v10→v1 迁移演练 17 项检查全通过且未触碰实时数据库。变更 Python 文件 `ruff check`、
  strict `mypy` 和 `git diff --check` 通过。本地全仓组合回归及全协议/Agent 文件仍会触发
  项目已记录的 AnyIO/asyncio 线程池不调度：已通过的定向文件可稳定独立重跑，完整异步链交由 CI 验证。

## 8. 6A Compose 关闭门禁

- 确定性 E2E Model 新增 `COMPOSE_6A_PARALLEL`：LLM Planner 返回互不依赖的
  `data.profile` 和 `stats.trend` 两个 ready steps，Executor 在一次 model turn 中收到画像与趋势
  两个 Tool Call，真实走 API → Gateway → `data-tools`/`stats-tools` Streamable HTTP 链路；
- Compose Playwright 直接核对两条 `step.started` 都先于任一 `step.completed`，分支节点不同、
  数据版本相同、Ledger 序号为 1/2，且任务终态时根节点已关闭、无活动分支；
- Compose 脚本在 Web/API/report-tools 重启后和离线工作区恢复后各重读一次并行 Run，确认
  `execution_control`、两条分支审计和 Evidence Ledger 不因进程/容器重建丢失；
- 本地新增关闭门禁定向后端回归 **12 条通过**，前端 Playwright **11 条通过、1 条
  Compose 专用场景按条件跳过**；前端 lint/build、Compose spec 发现、脚本语法、全仓 `ruff`、变更源文件
  strict `mypy` 与 `git diff --check` 通过。当前环境没有 Docker CLI，不伪称本地真实 Compose 已执行。

## 9. 6A CI 关闭证据

- 提交 `b67b704` 的 GitHub Actions
  [run 31348476642](https://github.com/Zacharyz26/EXCELChatBI/actions/runs/31348476642)
  已确认 `Backend tests and lint`、`Frontend build and browser E2E` 与
  `Build images and run Compose browser gate` 三个作业全部成功；
- Compose 门禁真实执行 API → Gateway → `data-tools`/`stats-tools` 的双分支并行，随后完成
  Web/API/report-tools 重启、MCP Resource 重连和离线工作区恢复；schema v10、共享预算、固定
  数据版本、取消树、分支审计和 Evidence Ledger 均通过恢复核对；
- 关闭过程中发现并修复统计趋势夹具的最小样本契约，以及宿主/容器重复夹具来源导致的漂移；
  回归测试现直接调用 Compose 实际文件生成入口并执行真实 `trend_analysis` 参数；
- 据此，阶段 6A 的工程范围正式关闭。该结论不替代真实 CPU/GPU semantic 等价、领域代表性
  场景、G7 人工盲评或负责人签字。

## 10. 6A 后续边界

1. 6A 已由完整 CI 关闭；下一开发入口为数据角色识别与质量建议；
2. 假设筛选、结果驱动分析、预测和多数据集关联在上述控制面稳定后逐项进入，不能
   用本次目录冻结提前宣称阶段 6 完成。
