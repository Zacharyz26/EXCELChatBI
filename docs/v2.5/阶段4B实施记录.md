# v2.5 阶段 4B 实施记录

> 状态：React 协作交互完成；阶段 4 仍在开发
> 更新日期：2026-07-31
> 前置：阶段 4A-1/4A-2 的服务端协作契约与审批执行链

## 1. 本阶段目标

阶段 4B 把 4A 已完成的 TaskRun、不可变计划修订、结构化澄清、运行控制和
ApprovalRecord 接入现有 React 对话工作区。前端只负责展示和提交用户决定，服务端
TaskRun 状态、`If-Match` 版本与 ApprovalRecord 始终是可信事实源。

本阶段不实现 SSE 游标重连、API/Compose 重启恢复和自主等级；这些仍属于后续 4C/4D。

## 2. 实际交付

### 2.1 类型化 API 与流式续跑

- 为 TaskRun、TaskPlan/TaskStep、TaskEvent、ApprovalRecord 和阻塞澄清增加前端类型；
- 接入任务详情、事件、审批列表、暂停、取消、计划修订和审批决定 API；
- 以 `fetch` 统一消费聊天、恢复、澄清回答和单步重试的 POST SSE，兼容 CRLF、分块边界
  与多行 `data`；
- 所有控制写操作发送服务端要求的 `If-Match` 和 `Idempotency-Key`；
- `/chat/stream` 可在响应头到达时读取 `X-ChatBI-Run-ID`，也能从 v2 TaskEvent 恢复
  当前 run。

### 2.2 工作区状态

- Zustand 工作区保存当前对话最近一次 TaskRun、计划/步骤、审批和待回答澄清；
- 发送消息、恢复、回答澄清和单步重试后统一刷新对话、Artifact、Dataset 与 TaskRun；
- 对话切换后按会话级 `sessionStorage` 恢复最近 run，读取失败时清除陈旧引用；
- 状态变化来自服务端详情或 TaskEvent；前端不会把本地按钮状态当成授权；
- 持久化会话快照落地后立即清除临时流式卡片，避免 TaskRun 刷新期间正文或 Artifact
  重复渲染。

### 2.3 统一任务协作面板

工作区标题栏新增“任务协作”入口和等待处理提示。面板集中提供：

- TaskRun 状态、状态版本、计划版本、目标和终态原因；
- 安全边界内的暂停、显式恢复和取消；
- 结构化澄清答案提交，并在同一个 `run_id` 上续跑；
- 暂停态完整计划编辑、步骤目的修改和步骤跳过；保存后显示不可变新版本；
- 失败/阻塞步骤的单步重试；
- 高风险审批的风险、工具、计划/步骤、有效期、工具契约 hash 和参数摘要 hash；
- 带原因的批准一次或拒绝。批准只更新 ApprovalRecord，任务仍保持暂停，必须再次
  显式点击“继续执行”。

面板已适配窄屏；浏览器不连接 MCP endpoint，也不持有 MCP 服务凭据。

## 3. 浏览器契约

本地 Playwright mock 按 4A 真实 API 形状校验 TaskRun 和 ApprovalRecord 版本。新增三条
核心场景：

1. 高风险调用在待审批时禁止恢复，批准后仍不自动执行，显式继续后批准变为已消费；
2. 暂停态计划修改生成 v2，不覆盖 v1，任务仍保持暂停；
3. `waiting_user` 澄清提交答案后继续原 TaskRun 并进入完成态。

原有 Excel 上传、Chart/Report Artifact 刷新恢复和知识库场景继续回归。

## 4. 验证结果

- `pnpm lint`：通过；
- `pnpm build`：通过；现有主包体积告警仍保留；
- `pnpm test:e2e`：8 条通过，其中 3 条为 4B 协作场景；
- `.venv/bin/pytest -q tests/test_collaboration.py`：5 条通过；
- 使用示例模型注册表和本地替身 RAG 的完整后端回归：504 条通过、2 条跳过。

## 5. 未完成边界

- 断线后的 `Last-Event-ID`/事件游标重连与去重尚未接入浏览器；
- 页面刷新只恢复同一浏览器会话记住的最近 run，尚未完成服务端“按对话查询最近 run”
  契约和跨浏览器恢复；
- API 重启、Web 代理重启、Compose 内审批/取消/报告下载的真实浏览器 E2E 尚未验收；
- 工具来源、权限和健康状态的完整前端视图尚未实现；
- 自主等级、分支对比、证据反馈闭环仍属于 4D；
- 当前未启用新的高风险生产工具，4B 不能被解释为扩大了生产工具权限。

## 6. 下一入口

1. 4C：实现 SSE 游标重连、重复事件抑制和按对话恢复最近 TaskRun；
2. 4C：补页面刷新、API/Web 重启、跨主体隔离和 Compose 真实浏览器 E2E；
3. 4C：补齐工具来源、权限、健康和 Evidence/Artifact 的审计一致性展示；
4. 4D：落地自主等级、分支对比与反馈闭环，完成阶段 4 发布门禁。
