# v2.5 记忆、自主性与协作

> 状态：阶段 3A–3C 已完成；3D/3E 本地实现完成并等待提交后的 CI/Compose 门禁
> 更新日期：2026-07-31
> 基线：v2.4 阶段 2A–2E 已实现，最新 Compose/容器 CI 全绿；G7 因人工评测
> 代表性不足继续保持 `review_required`

本目录把 [`Agent自主化开发规划.md`](../Agent自主化开发规划.md) 中的 v2.5
阶段 3–6 拆成可实施、可验收的交付批次。进入 v2.5 不表示豁免 v2.4 的 G7：
现有 20 个行为场景全部使用订单、销售、利润、地区、渠道或复购率等商业分析语境，
可以继续作为控制面回归，但不足以证明产品适合尚未明确的真实工作负载。剩余人工盲评
暂缓，已完成的本地 Planner 评分只作为评分校准和商业场景回归证据，不用于伪造代表性
验收结论。

## 阶段总览

| 阶段 | 目标 | 当前决策 |
|---|---|---|
| 3：记忆系统 | 工作/对话/项目记忆、上下文压缩、指代消解、Evidence 血缘与恢复 | 立即实施；先做领域无关的 3A |
| 4：人机协作 | 计划干预、审批、任务控制、证据与风险展示、前端恢复 | 阶段 3 基础稳定后实施 |
| 5：知识与数据联合推理 | 版本化领域定义、公式、口径冲突与知识 Resource | 等真实需求逐渐明确；不预置销售领域 |
| 6：自主分析 | 数据角色、结果驱动分析、多数据集、受控并行与统计护栏 | 等代表性场景冻结后实施和验收 |

受限 SQL 和 Code Interpreter 仍属于独立安全项目，不随 v2.5 普通功能自动启用。

## 当前实施入口

第一批为
[`阶段3A实施计划.md`](./阶段3A实施计划.md)：先建立记忆契约、正式迁移、
Memory Policy、Repository、不可变快照和项目隔离。该批不让模型获得通用
`memory.write` 工具，也不自动把对话内容写成长期事实。

当前代码与验证状态见
[`阶段3A实施记录.md`](./阶段3A实施记录.md)。提交 `162b170` 的完整 CI（含 Docker
联合破坏/恢复门禁）已经全绿，3A-1–3A-4 正式关闭。

3B 历史实施入口为
[`阶段3B实施计划.md`](./阶段3B实施计划.md)：把现有“最近 N 条截断”升级为带来源
范围、版本、hash、项目隔离和 TaskRun 固定引用的持久化上下文压缩。实现与验证明细见
[`阶段3B实施记录.md`](./阶段3B实施记录.md)。提交 `e3d51fd` 的真实
Docker/Compose CI 全绿后，3B 已关闭。

3C 历史实施入口为
[`阶段3C实施计划.md`](./阶段3C实施计划.md)：先建立受项目隔离的 Artifact/Dataset
确定性引用目录、歧义失败关闭、TaskPlan 绑定和工具血缘约束，再以固定 MemorySnapshot
接入严格结构化的已确认实体映射。领域中立质量集、stdio/Streamable HTTP 探针和
真实 Compose 恢复门禁均已通过，3C 已关闭。证据见
[`阶段3C实施记录.md`](./阶段3C实施记录.md)。

3D 实施入口为
[`阶段3D实施计划.md`](./阶段3D实施计划.md)：提供项目记忆的安全查询、不可变纠正、
软删除和前端治理入口，同时保留原始来源、资源关联、固定快照和审计。3D-1–3D-3
已完成本地实现；后端 `486 passed, 2 skipped`、前端 lint/build、普通浏览器 E2E 和
真实本机 Web/API 治理链路已通过。当前实现与发布边界见
[`阶段3D实施记录.md`](./阶段3D实施记录.md)；真实 Docker/Compose 门禁仍需提交后的
CI runner 确认。

当前开发入口为
[`阶段3E实施计划.md`](./阶段3E实施计划.md)：以 SQLite v6 不可变 Dataset 锚点和现有
Invocation/Evidence/Claim 真相表派生五阶段来源图，提供安全查询与 React 查看，并把图
hash/计数纳入 readiness、离线备份、API 重启和 Compose 恢复探针。3E-1–3E-3 已完成
本地实现；后端 `497 passed, 2 skipped`、六场景领域中立质量门禁、前端 lint/build、
普通浏览器和真实本机 full-stack E2E 已通过。证据见
[`阶段3E实施记录.md`](./阶段3E实施记录.md)；真实 Docker/Compose 门禁仍需提交后的
CI runner 确认。

阶段 3 按以下顺序推进：

1. **3A 契约与持久化**：已完成；
2. **3B 上下文压缩**：已完成，持久压缩、质量、并发和 Compose 恢复门禁全绿；
3. **3C 指代消解**：已完成，对象/记忆解析、23 场景质量门禁、双 MCP 传输和固定计划
   恢复探针全绿；
4. **3D 用户治理**：本地实现完成；安全查询、不可变纠正、软删除、审计和真实本机
   full-stack E2E 已通过，等待提交后的真实 Compose CI 后关闭；
5. **3E 血缘与恢复**：本地实现完成；已贯通 Dataset → Analysis → Artifact →
   Evidence → Claim，并把稳定图 hash 纳入备份和重启恢复；等待提交后的真实 Compose
   runner 后关闭。

## 评测与发布纪律

- v2.4 的单元、协议、迁移、浏览器和 Compose 回归继续执行，不因盲评暂缓而跳过；
- 阶段 3–4 使用领域中立场景验证“第二个 Artifact”“刚才确认的字段”“删除旧别名”、
  跨项目隔离和重启恢复，不依赖利润/销售额等专用字段；
- 阶段 5–6 开始前，另建真实工作负载场景集；只需匿名 schema、典型请求、Required
  Artifact、澄清条件和禁止行为，不要求保存敏感原始数据；
- 现有商业场景保留为 `business regression`，不得与新的代表性验收集混合平均；
- 新场景、评分口径、重复次数和 hash 必须在运行前冻结；查看失败样本后的改进只能使用
  新的未见场景复评；
- 在人工评测、负责人签字和 ADR 接受前，v2.4 G7 状态继续为 `review_required`。

## 相关设计

- [`../Agent自主化开发规划.md`](../Agent自主化开发规划.md)：版本和阶段的唯一现行路线图；
- [`../MCP与Docker全阶段演进设计.md`](../MCP与Docker全阶段演进设计.md)：
  v2.5/v3.0 的 MCP、容器和安全边界；
- [`../v2.4/README.md`](../v2.4/README.md)：v2.4 已实现范围和未关闭门禁；
- [`../v2.4/Planner与Verifier评测设计.md`](../v2.4/Planner与Verifier评测设计.md)：
  现有冻结评测及代表性修订要求。
