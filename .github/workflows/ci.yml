name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend tests and lint
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up uv and Python
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          python-version: "3.11"
          enable-cache: true

      - name: Install backend dependencies
        run: uv sync --frozen --extra stats --extra report --extra rag-store

      - name: Run backend tests
        run: uv run --frozen pytest -q
        env:
          MODEL_REGISTRY_PATH: config/models.example.yaml

      - name: Run deterministic knowledge quality gate
        run: >-
          uv run --frozen python scripts/kb_eval.py
          --enforce --profile baseline --json-output .data/kb-eval.json
        env:
          RAG_EMBEDDER: hashing
          RAG_RERANKER: lexical
          RAG_STORE: local
          RAG_MIN_RELEVANCE: "0.0"

      - name: Upload knowledge evaluation report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v5
        with:
          name: kb-evaluation
          path: .data/kb-eval.json
          if-no-files-found: error
          retention-days: 30

      - name: Run Ruff
        run: uv run --frozen ruff check .

      - name: Validate Milvus Standalone Compose
        run: >-
          docker compose --env-file deploy/milvus/.env.example
          -f deploy/milvus/docker-compose.yml config --quiet

  frontend:
    name: Frontend build and browser E2E
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: apps/web
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: pnpm
          cache-dependency-path: apps/web/pnpm-lock.yaml

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Type-check frontend
        run: pnpm lint

      - name: Build frontend
        run: pnpm build

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Run browser E2E
        run: pnpm test:e2e

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v5
        with:
          name: playwright-report
          path: |
            apps/web/playwright-report/
            apps/web/test-results/
          if-no-files-found: ignore
          retention-days: 14
